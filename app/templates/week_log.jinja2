<!DOCTYPE html>
<html lang="tr">
<head>
    <title>GreenChute - Haftalık Rapor</title>
    <meta charset="utf-8">
    <link href="favicon.ico" rel="icon" type="image/x-icon"/>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;700&display=swap');

        body {
            font-family: 'Poppins';
            font-size: 1.2em;
        }

        a {
            color: black;
            text-decoration: none;
            cursor: auto;
        }

        ul {
            line-height: 3.7em;
        }

        .big {
            font-size: 1.5em;
            font-weight: bold;
        }

        .wrapper {
            text-align: center;
        }

        .box {
            width: 25em;
            height: fit-content;
            border-radius: 1.5em;
            border: 1px solid #ccc;
            margin: 6.5vh 5vh 3vh;
            display: inline-block;
            vertical-align: top;
            transition: .3s;
        }

        .box:hover {
            box-shadow: 0 0 11px rgba(33, 33, 33, .2);
        }

    </style>
</head>
<body>
<div class="wrapper">
    {% for day, i in zip(["Pazartesi", "Salı", "Çarşamba", "Perşembe", "Cuma", "Cumartesi", "Pazar"], range(7)) %}
    <div class="box">
        <p class="big">{{ day }}</p>
        <ul>
            {% for j, k in zip(range(3), ["Geri Dönüşen Atık", "Geri Dönüşmeyen Atık", "Kompost"]) %}
            <li><b>{{ k + ": " }}</b>{{ w[i][j] }}</li>
            {% endfor %}
        </ul>
    </div>
    {% endfor %}
</div>
</body>
</html>
